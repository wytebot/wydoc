import React,{useState} from 'react'; import {NavLink,useNavigate} from 'react-router-dom'; import {LayoutDashboard,ShieldCheck,Globe2,Bell,BookOpen,ChevronDown,CreditCard,HelpCircle,Settings,LogOut,Menu,X,Info,LockKeyhole} from 'lucide-react'; import {signOut,auth} from '../lib/firebase';
const links=[['/','Dashboard',LayoutDashboard],['/issues','Issues',ShieldCheck],['/sites','Sites',Globe2],['/notifications','Notifications',Bell],['/how-it-works','How it works',BookOpen],['/billing','Billing',CreditCard]];
export default function Layout({children,user,site,setSite,sites}){const [mobile,setMobile]=useState(false);const nav=useNavigate();return <div className="shell"><aside className={'sidebar '+(mobile?'mobileOpen':'')}><div className="brand"><img src="/icon-192.png" alt="" className="brandMark"/>Wy<span>Doc</span></div><div className="siteBox"><div className="muted">ACTIVE SITE</div><button onClick={()=>nav('/sites')} className="siteSelect">{site?.name||'Add your first site'} <ChevronDown size={16}/></button></div><nav>{links.map(([p,l,I])=><NavLink key={p} to={p} end={p==='/' } onClick={()=>setMobile(false)}><I size={18}/><span>{l}</span></NavLink>)}</nav><div className="navBottom"><NavLink to="/settings"><Settings size={18}/>Settings</NavLink><NavLink to="/about"><Info size={18}/>About</NavLink><NavLink to="/privacy"><LockKeyhole size={18}/>Privacy</NavLink><NavLink to="/terms"><HelpCircle size={18}/>Terms</NavLink><button onClick={()=>signOut(auth)}><LogOut size={18}/>Sign out</button></div></aside><main className="main"><header className="topbar"><button className="iconBtn mobileOnly" onClick={()=>setMobile(!mobile)}>{mobile?<X/>:<Menu/>}</button><div className="crumb">{site?.name||'Workspace'}</div><div className="topActions"><button className="bell" onClick={()=>nav('/notifications')}><Bell size={19}/>{site?.unread? <i/>:null}</button><div className="avatar">{user?.displayName?.[0]||user?.email?.[0]||'U'}</div></div></header><section className="content">{children}</section><nav className="bottomNav">{links.slice(0,4).map(([p,l,I])=><NavLink key={p} to={p} end={p==='/' }><I size={19}/><small>{l}</small></NavLink>)}</nav></main></div>}
