import React,{useEffect,useState} from 'react'; import {createRoot} from 'react-dom/client'; import {BrowserRouter} from 'react-router-dom'; import {onAuthStateChanged,auth} from './lib/firebase'; import App from './App'; import './styles.css';
function Root(){const [user,setUser]=useState(undefined); useEffect(()=>{onAuthStateChanged(auth,setUser); if('serviceWorker'in navigator){navigator.serviceWorker.register('/wydoc-sw.js',{updateViaCache:'none'}).then(r=>r.update()).catch(()=>{});}},[]); if(user===undefined)return <div className="splash"><img src="/icon-192.png" alt="" width="56" height="56" style={{borderRadius:14,marginBottom:14}}/>WyDoc</div>; return <App user={user}/>}
createRoot(document.getElementById('root')).render(<BrowserRouter><Root/></BrowserRouter>);
