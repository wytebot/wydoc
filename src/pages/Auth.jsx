import React,{useState} from 'react'; import {signInWithPopup,auth,googleProvider} from '../lib/firebase'; import {ShieldCheck,Globe2,Bell} from 'lucide-react';
export default function Auth(){const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function login(){setError('');setBusy(true);try{await signInWithPopup(auth,googleProvider)}catch(e){if(e?.code==='auth/popup-closed-by-user'||e?.code==='auth/cancelled-popup-request'){}else setError(e?.code==='auth/popup-blocked'?'Your browser blocked the sign-in popup. Please allow popups for this site and try again.':e.message||'Could not sign in. Please try again.')}finally{setBusy(false)}}return <div className="auth"><div className="authCard"><div className="brand big">Wy<span>Doc</span></div><h1>Know when your content needs attention.</h1><p className="muted">Verify important claims, detect stale content and get alerts without storing your whole website forever.</p><div className="pills"><span><ShieldCheck size={16}/>Claim verification</span><span><Globe2 size={16}/>Site monitoring</span><span><Bell size={16}/>Web push alerts</span></div><button className="primary wide" disabled={busy} onClick={login}>{busy?'Opening sign-in…':'Continue with Google'}</button>{error&&<div className="error">{error}</div>}<p className="tiny">By continuing, you agree to the Terms and Privacy Policy.</p></div></div>}
