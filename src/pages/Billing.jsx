import React,{useState} from 'react'; import {Check} from 'lucide-react'; import {auth} from '../lib/firebase';
const plans=[['Free','₦0','1 site','100 verification claims/month','7-day history'],['Pro','₦9,900/mo','Multiple sites','2,500 verification claims/month','90-day history','Priority monitoring']];
export default function Billing(){const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [justPaid]=useState(()=>new URLSearchParams(window.location.search).get('payment')==='complete');async function pay(){if(!auth.currentUser)return;setBusy(true);setError('');try{const token=await auth.currentUser.getIdToken();const r=await fetch('/api/flutterwave/checkout',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({plan:'pro'})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Checkout failed');if(!d.link)throw new Error('Flutterwave did not return a checkout link.');window.location.assign(d.link)}catch(e){setError(e.message||'Could not start checkout');setBusy(false)}}return <><div className="pageHead"><div><div className="eyebrow">BILLING</div><h1>Simple plans</h1><p className="muted">Pay securely with Flutterwave. Pro access is granted only after server-side verification.</p></div></div>{justPaid&&<div className="verifyResult"><Check/><div><b>Payment received</b><span>We're verifying it with Flutterwave — your plan updates automatically within a few seconds.</span></div></div>}{error&&<div className="error">{error}</div>}<div className="plans">{plans.map((p,i)=><div className={'plan card '+(i?'featured':'')} key={p[0]}><div className="planName">{p[0]} {i===1&&<span className="badge">POPULAR</span>}</div><div className="price">{p[1]}</div>{p.slice(2).map(x=><div className="feature" key={x}><Check size={16}/>{x}</div>)}{i===1&&<button className="primary wide" disabled={busy} onClick={pay}>{busy?'Opening checkout…':'Upgrade with Flutterwave'}</button>}</div>)}</div><div className="card"><h3>Billing security</h3><p className="muted">Payment secrets stay server-side. Never paste a Flutterwave secret key into a VITE_ variable or your browser.</p></div></>}
