import webpush from 'web-push'; import {bearerUser,adminDb} from './_firebase-admin.js';
export default async function handler(req,res){try{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const user=await bearerUser(req);if(!process.env.VAPID_PRIVATE_KEY||!process.env.VAPID_EMAIL)return res.status(200).json({ok:false,skipped:true});webpush.setVapidDetails(process.env.VAPID_EMAIL.startsWith('mailto:')?process.env.VAPID_EMAIL:`mailto:${process.env.VAPID_EMAIL}`,process.env.VAPID_PUBLIC_KEY||process.env.VITE_VAPID_PUBLIC_KEY,process.env.VAPID_PRIVATE_KEY);const snap=await adminDb.collection('pushSubscriptions').where('ownerId','==',user.uid).get();let sent=0;for(const d of snap.docs){try{await webpush.sendNotification(d.data().subscription,JSON.stringify({title:'Welcome to WyDoc 👋',body:'Your workspace is ready. Add a website and run your first content health scan.',url:'/',tag:'wydoc-welcome'}));sent++}catch(e){if(e.statusCode===404||e.statusCode===410)await d.ref.delete()}}return res.status(200).json({ok:true,sent})}catch(e){return res.status(500).json({error:e.message||'Welcome notification failed'})}}
