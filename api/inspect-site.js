import * as cheerio from 'cheerio';
function clean(u){const x=new URL(u);if(!['http:','https:'].includes(x.protocol))throw new Error('Only HTTP(S) URLs are supported.');x.hash='';return x.toString().replace(/\/$/,'')}
function blocked(host){const h=host.toLowerCase();return h==='localhost'||h==='127.0.0.1'||h==='0.0.0.0'||h==='::1'||h.endsWith('.localhost')||/^10\./.test(h)||/^192\.168\./.test(h)||/^172\.(1[6-9]|2\d|3[0-1])\./.test(h)}
export default async function handler(req,res){try{if(req.method&&req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const raw=req.query?.url;if(!raw)return res.status(400).json({error:'URL is required'});const url=clean(/^https?:\/\//i.test(raw)?raw:`https://${raw}`);const parsed=new URL(url);if(blocked(parsed.hostname))return res.status(400).json({error:'Private or local addresses cannot be inspected.'});const r=await fetch(url,{redirect:'follow',headers:{'user-agent':'WyDocBot/1.0 (+content verification)','accept':'text/html,application/xhtml+xml'}});if(!r.ok)throw new Error(`Site returned HTTP ${r.status}`);const final=new URL(r.url);if(final.origin!==parsed.origin||blocked(final.hostname))throw new Error('Redirected to a different or private host.');const html=await r.text();const $=cheerio.load(html);const name=($('meta[property="og:site_name"]').attr('content')||$('meta[name="application-name"]').attr('content')||$('title').text().trim()||final.hostname).slice(0,120);const urls=new Set();const add=u=>{try{const x=new URL(u,final);if(x.origin!==final.origin||x.hash||!/^https?:$/.test(x.protocol))return;const p=x.pathname.toLowerCase();if(/(blog|news|article|post|story|\d{4}\/\d{2})/.test(p))urls.add(x.toString().replace(/\/$/,''));}catch{}};$('a[href]').each((_,el)=>add($(el).attr('href')));for(const path of ['/sitemap.xml','/sitemap_index.xml']){try{const sr=await fetch(new URL(path,final),{headers:{'user-agent':'WyDocBot/1.0'}});if(sr.ok){const txt=await sr.text();const sm=cheerio.load(txt,{xmlMode:true});sm('loc').each((_,el)=>add(sm(el).text().trim()));}}catch{}}return res.status(200).json({ok:true,url:final.toString().replace(/\/$/,''),name,articleCount:Math.min(urls.size,500),discovered:Array.from(urls).slice(0,100)})}catch(e){return res.status(500).json({error:e.message||'Inspection failed'})}}
